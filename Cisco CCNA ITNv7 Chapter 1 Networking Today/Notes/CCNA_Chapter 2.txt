CCNA 1 - Chapter 2
==================

Configuring a Switch or Router
	conneting -- Console cable (Serial), Ethernet cable (SSH, Telnet)
	Consoling -- Patty, Tera Term, SecureCRT
Cisco IOS -- Cisco router and switch operating sysyem

Switch>			- User Exec mode        (Pasword to access device)
Switch#			- Privileged Exce mode  (Passworw configure device)
Switch(config)#		- Globale configuration mode
Switch(config-line)#    - Line configuration mode
Switch(config-if)#	- Interface configuration mode



Switch> enable
Switch# cofigure 	terminal
Switch(config)# 	hostname S1
S1(config)# line 	console 0
S1(config-line)# 	password cisco
S1(config-line)# 	login
S1(config-line)# 	exit
			end
			ctrl+c
			ctrl+z
S1(config)# enable secret class
S1(config)# line vty 0 15
S1(config-line)# password cisco
S1(config-line)# login
S1(config-line)# exit
S1(config)#	 service password encription
S1(config)# banner motd "no unauthorized access allowed!" or #
S1(config)# interface vlan 1
S1(config-if)# ip address 192.168.0.2 255.255.255.0
S1(config-if)# no shurdown
S1(config-if)# end
S1(config)# ip default - gateway 192.168.0.1
S1(config)# exit 
S1# copy running-config startup-config
S1# show running-config
S1# show startup-config
s1# show ip interface brief




Switch>								--user Exec mode
Switch> enable
Switch#								--privileded Exec mode
Switch# disable
Switch>


Switch# cofigure 	terminal		
Switch(config)#					--global configuration mode
Switch(config)# exit				--exit, end, ctrl+c, ctrl+z
Switch#	

Switch#	 con?						--command completion with ?
configre connect

Switch# configre ?					--command hinting with ?
	termimal configure from the terminal
	<cr>

Switch# codf t						--command shorteing
Switch(config)#





